/**
 * Handles one normalized value.
 *
 * @jscallback{jsdocvirtualcallbackuhlalnldlllelr||Handler}
 * @param value Normalized value supplied to the callback. Type: string.
 * @returns True when the value is accepted. Type: boolean.
 */

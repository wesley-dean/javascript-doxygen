/**
 * Format a value using the preferred formatter.
 */
function formatValue(value) {}

/**
 * Format a value using the legacy formatter.
 *
 * @deprecated Use formatValue instead.
 * @see formatValue
 */
function legacyFormat(value) {}

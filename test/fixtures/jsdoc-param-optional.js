/**
 * Greet a user with an optional salutation.
 *
 * @param {string} [salutation=Hello] - Salutation placed before the name.
 */
function greet(salutation = "Hello") {
  return `${salutation}, world!`;
}

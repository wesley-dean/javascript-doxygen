---
# yamllint disable-line rule:truthy
on:
  workflow_dispatch:

name: Versioning (deferred)
permissions:
  contents: read

jobs:
  deferred:
    runs-on: ubuntu-24.04
    steps:
      - name: Report deferred release automation
        run: |
          printf '%s\n' \
            'Release automation is deferred until a JavaScript consumer artifact exists.'

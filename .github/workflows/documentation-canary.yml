---
name: Documentation canary (deferred)

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deferred:
    runs-on: ubuntu-24.04
    steps:
      - name: Report deferred documentation integration
        run: |
          printf '%s\n' \
            'Documentation canary is deferred until JavaScript/Doxygen integration is implemented.'
